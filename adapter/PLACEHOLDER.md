# Adapter 0.1.1 placeholder

This directory is reserved for the `adapter` **0.1.1** source drop.

The follow-up payload will replace this placeholder with the actual adapter file
bodies. The full upstream `effect-fabric` and
`function-hooks-core-reference` source trees are intentionally not vendored into
git for this freeze.
