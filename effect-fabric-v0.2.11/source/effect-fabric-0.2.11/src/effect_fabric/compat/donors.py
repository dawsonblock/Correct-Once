from __future__ import annotations

DONOR_ROLES: dict[str, str] = {
    "durable-agent-outbox": "pure reducer and adversarial conformance semantics",
    "OpenOnce": "provider capability and honest reconciliation semantics",
    "AgentAction": "exact-action authorization evidence and MCP policy interoperability",
    "PALOframework": "Effect Contract, one-use capability, verifier, incident/hold semantics",
    "ChronoMCP": "MCP reversibility and deterministic compensation metadata",
}
