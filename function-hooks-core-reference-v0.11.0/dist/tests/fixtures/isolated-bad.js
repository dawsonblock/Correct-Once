import { readFileSync } from "node:fs";
export function register(on) {
    void readFileSync;
    on("prompt.submit", async (_$, event, next) => next(event));
}
//# sourceMappingURL=isolated-bad.js.map