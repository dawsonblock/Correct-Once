# @function-hooks/enterprise

Experimental managed-policy package. It provides compatibility registrar policies plus stable-kernel blueprint filtering and explicit origin allowlist hooks. Diagnostic origin labels are not treated as authentication identities.
